using System;
//using Hazelcast.Config;
using Hazelcast.Core;
//using Hazelcast.Client;


namespace ST4_ImplementationExamples
{
    public class HazelcastClientTemplate
    {/*
       static void Communicate ()  { 
           // get Hazelcast instance
       HazelcastInstance hz = getHazelcastInstance();
       // publish to topic, creates a new if it doesn't exist
       ITopic<String> topic = hz.getTopic("ExampleTopic");
       topic.publish("Example data");
       Console.WriteLine("Message published by topic publisher HazelcastTemplatePublisher");
    }
    private static HazelcastInstance getHazelcastInstance(){
        //hazelcast address for the clients
        var clientConfig = new ClientConfig();
        // don't know if port can be used here
        clientConfig.GetNetworkConfig().AddAddress("127.0.0.1:5701");
        return HazelcastClient.newHazelcastClient(clientConfig);
    }
   */
    }
}